# 조직개편 반영 가이드 — v6.1 (콘텐츠기획팀 / 퍼블리싱자동화팀)

> 변경 범위: **팀 목록만** 변경했습니다. 표 구조·색상·기능·엑셀 다운로드·필터·검색 등 나머지 로직은 원본 그대로입니다.
> 폐지된 **디지털마케팅팀 / NE Times팀**은 코드에서 **삭제하지 않고 남겨두었습니다.**
> → 6M~9M-1W 과거 주차 시트를 열면 예전처럼 그대로 보입니다. (해당 주차에 행이 없으면 섹션·탭이 자동으로 숨겨짐)

---

## 1. GitHub → Vercel (프론트엔드)

새 저장소 최상단에 아래 3개를 **같은 이름 그대로** 덮어쓰기 → 커밋하면 Vercel이 자동 재배포합니다.

```
index.html
style.css
app.js
```

⚠️ `app.js` 맨 위의 `API_URL` 은 **지금 쓰는 값 그대로** 들어 있습니다. 수정 불필요.

변경된 부분 (참고)
| 파일 | 변경 내용 |
|---|---|
| app.js | `TEAM_ORDER` / `TEAM_DISPLAY` / `TEAM_META` 에 content·publishing 추가, 탭 표시 로직을 `TEAM_ORDER` 기준으로 자동화 |
| style.css | `--team-content` / `--team-publishing` 색상 변수 + `.t-content` / `.t-publishing` 규칙 추가 |
| index.html | 상단 탭에 `콘텐츠기획`, `퍼블리싱자동화` 추가 (데이터 없는 탭은 자동 숨김) |

표시 순서: **콘텐츠기획 → 서비스운영 → 서비스기획 → 퍼블리싱자동화 → AX**
(과거 주차는 예전 순서 그대로: 디지털마케팅 → 서비스운영 → 서비스기획 → NE Times → AX)

---

## 2. Apps Script (Code.gs)

### 권장 — 2군데만 바꾸기 (가장 안전)

지금 배포된 Code.gs에서 아래 **두 블록만** 찾아서 교체하세요.

**① `const TEAM_KEYS = { … };` 전체를 교체**

```js
const TEAM_KEYS = {
  '콘텐츠기획팀':     'content',
  '서비스운영팀':     'operations',
  '서비스기획팀':     'planning',
  '퍼블리싱자동화팀': 'publishing',
  'AX팀':             'ax',
  '디지털마케팅팀':   'marketing',   // (폐지) 과거 주차 표시용
  'NE Times팀':       'netimes',     // (폐지) 과거 주차 표시용
};
```

**② `parseTeamItems` 안의 `const teams = { … };` 전체를 교체**

```js
  const teams = {
    content:    { code: 'content',    name: '콘텐츠기획팀',     items: [] },
    operations: { code: 'operations', name: '서비스운영팀',     items: [] },
    planning:   { code: 'planning',   name: '서비스기획팀',     items: [] },
    publishing: { code: 'publishing', name: '퍼블리싱자동화팀', items: [] },
    ax:         { code: 'ax',         name: 'AX팀',             items: [] },
    marketing:  { code: 'marketing',  name: '디지털마케팅팀',   items: [] },  // (폐지) 과거 주차 표시용
    netimes:    { code: 'netimes',    name: 'NE Times팀',       items: [] },  // (폐지) 과거 주차 표시용
  };
```

### 대안 — 파일 통째로 교체
같이 드린 `Code.gs`(v6.1)를 붙여넣어도 됩니다.
단, 업로드해주신 Code.gs 파일이 `isShown` 함수 중간에서 **잘려 있었기** 때문에,
그 뒤에 있던 `parseProgress` / `parseDecisions` 는 시트 구조(우선순위·타이틀·본문·필요 액션·마감일)에 맞춰 **다시 작성한 버전**입니다.
현재 배포본이 정상 동작 중이라면 **위의 "2군데만 교체" 방식을 권장**합니다.

### 재배포 (URL 유지 — app.js 수정 불필요)
`배포 > 배포 관리 > (연필 아이콘) 편집 > 버전: 새 버전 > 배포`

---

## 3. 구글시트에서 해야 할 일

### 3-1. 팀 드롭다운 목록 수정 — `4. 팀별 주요 실적` B열
`데이터 > 데이터 확인(유효성 검사)` 목록에 아래를 넣습니다.

```
콘텐츠기획팀
서비스운영팀
서비스기획팀
퍼블리싱자동화팀
AX팀
```

- 과거 시트를 보호하려면 옵션은 **"경고 표시"** 로 두거나, 목록에 `디지털마케팅팀` / `NE Times팀` 을 남겨두세요.
- 파트를 구분할 때는 **`팀명 / 파트명`** 형식(슬래시 구분)을 그대로 유지합니다. 예: `콘텐츠기획팀 / 커머스파트`
- **팀명 철자가 코드와 1글자라도 다르면 그 행은 대시보드에 안 나옵니다.** (공백·띄어쓰기 주의)

### 3-2. 현재 `9M-1W` 시트의 남아있는 옛 팀명 정리
노출설정이 **TRUE(체크)** 인데 아직 옛 팀명인 행 — 아래가 대상입니다.

| 현재 팀명 | 항목 | 권장 변경 |
|---|---|---|
| 디지털마케팅팀 / 커머스파트 | 교강사 대상 세미나 운영 | 콘텐츠기획팀 |
| 디지털마케팅팀 / 커머스파트 | 신간 런칭 작업 | 콘텐츠기획팀 |
| 디지털마케팅팀 / 플랫폼파트 | 제6회 NELT 경진대회 응시 촉진 | 콘텐츠기획팀 |
| 디지털마케팅팀 / 플랫폼파트 | 문법문제뱅크 사용자 확대 | 콘텐츠기획팀 |
| NE Times팀 | 커뮤니티 커머스 이벤트 (지나샘) | 콘텐츠기획팀 |
| NE Times팀 | 커뮤니티 커머스 이벤트 (막퍼주는영어) | 콘텐츠기획팀 |
| NE Times팀 | 9월 이벤트 협의 예정 | 콘텐츠기획팀 |
| NE Times팀 | NE TIMES 조판 자동화 (현재 FALSE) | 퍼블리싱자동화팀 |
| 서비스기획팀 | NE Times 조판 자동화 | (업무 이관 시) 퍼블리싱자동화팀 |

※ 어느 팀으로 갈지는 실제 R&R 기준으로 정하시면 됩니다. **코드는 어느 쪽이든 그대로 동작**합니다.
※ 노출설정이 FALSE인 행은 대시보드에 안 나오므로 급하지 않습니다.

### 3-3. `3. 월별 매출현황` 표의 팀 라벨 (표시용 텍스트)
A열 라벨은 **입력한 글자 그대로** 화면에 나옵니다. 파싱 로직과 무관하니 자유롭게 바꾸세요.

```
디지털마케팅팀 커머스파트(자사몰)  →  콘텐츠기획팀 커머스파트(자사몰)
디지털마케팅팀 플랫폼파트(NELT)    →  콘텐츠기획팀 플랫폼파트(NELT)
디지털마케팅팀 커머스파트(CP)      →  콘텐츠기획팀 커머스파트(CP)
NE Times팀                         →  (신규 담당팀명)
```
- 표 위의 `※ 디지털마케팅팀 / NE Times팀은 …` 주석 문구도 같이 손보면 좋습니다.
- **`합계` 라는 단어는 그대로 두세요.** 합계 행 인식에 쓰입니다.

### 3-4. 시작일/종료일(J·K열)은 '날짜' 서식 권장
`2026. 9. 2` 처럼 **날짜 셀**로 입력하면 화면에 `09.02` 로 정확히 표시됩니다.
`2026.8.28` 같이 **텍스트**로 입력하면 변환이 어긋날 수 있습니다(`26.08`). 비워두면 `-` 로 나옵니다.

### 3-5. 새 주차 시트 만들 때
가장 왼쪽 시트를 복사 → 이름을 `9M-2W` 형식으로 변경 → 팀명이 모두 신규 팀명인지 확인.

---

## 4. 배포 후 확인 체크리스트

- [ ] 상단 탭에 **콘텐츠기획 / 퍼블리싱자동화** 가 보이고, 클릭 시 해당 섹션으로 이동
- [ ] 옛 주차(예: `8M-4W`) 선택 시 **디지털마케팅팀 / NE Times팀 섹션이 예전처럼 정상 표시**
- [ ] 팀 헤더 좌측 색상 점(콘텐츠기획=보라, 퍼블리싱자동화=주황) 표시
- [ ] 본부 요약 스트립(총 과제·평균 진척·완료·진행중·이슈·핵심) 숫자 집계
- [ ] 전체 / 핵심만 / 이슈만 필터, 과제 검색, 펼치기·접기, 인쇄 정상
- [ ] `기존 폼으로 다운로드` 엑셀에 신규 팀명이 `[콘텐츠기획팀]` 형태로 출력
- [ ] 월별 매출·의사결정 요청 섹션 정상

> 문제가 생기면: Vercel > Deployments > 직전 배포 `⋯` > **Promote to Production** (즉시 롤백)
> 백엔드는 Apps Script `배포 관리`에서 이전 버전 선택.
